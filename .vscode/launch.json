{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "(OSX) Launch",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceRoot}/target/debug/rip_tree",
            "args": [],
            "cwd": "${workspaceRoot}",
        }
    ]
}